/**
 * Each individual test is run as a separate node.  Those nodes are added to
 * a thread pool to be executed.  This allows for maximum parallel execution
 * depending on the architecture of the system.
 */
package org.dhaven.jue.internal.node;